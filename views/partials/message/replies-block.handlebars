
                <div class="row flirt-{{isFlirt}}">
                    <div class="col-sm-2">
                        <h6>{{title}}</h6>
                        <img src={{user.image}}>
                    </div>
                    <div class="col-sm-10 {{send-recieve}}">
                        <div class="well">
                            <h4>{{user.name}}</h4>
                            <p>{{text}}</p>
                        </div>
                    </div>
  
                </div>